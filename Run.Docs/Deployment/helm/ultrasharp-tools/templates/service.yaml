apiVersion: v1
kind: Service
metadata:
  name: {{ include "ultrasharp-tools.fullname" . }}
  labels:
    {{- include "ultrasharp-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "ultrasharp-tools.selectorLabels" . | nindent 4 }}

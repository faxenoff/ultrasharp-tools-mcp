apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ultrasharp-data-pvc
  labels:
    app: ultrasharp-tools-server
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Optional: Specify storage class
  # storageClassName: fast-ssd
